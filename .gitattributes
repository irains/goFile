# Vite hashes source bytes; keep frontend inputs and committed output LF everywhere.
web/** text=auto eol=lf
# Generated frontend bundles may preserve upstream formatter whitespace.
assets/web/** text=auto eol=lf -whitespace
