<!DOCTYPE html>
<html lang="{{.htmlLang}}" data-bs-theme="dark">
{{template "public/header" .}}
<body><main class="app-shell"><a class="brand" href="{{appPath "/"}}"><span class="brand-mark">› FileHarbor</span></a><section class="empty-state mt-4"><p>{{.msg}}</p><a class="btn btn-primary" href="{{appPath .url}}">{{.title}}</a></section></main></body>
</html>
